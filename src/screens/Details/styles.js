import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  screen: {
    alignItems: 'center',
    backgroundColor: '#ddd',
    height: '100%',
    justifyContent: 'center',
  },
})
