import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  spinner: {
    resizeMode: 'contain',
    width: '30%',
  },
  wrapper: {
    alignItems: 'center',
    height: '80%',
    justifyContent: 'center',
  },
})
